<?xml version="1.0" encoding="utf-8"?>
<!-- Outlined turquoise circle — used for selected day (not today) -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/colorCalendarLight" />
    <stroke
        android:width="1.5dp"
        android:color="@color/colorCalendar" />
</shape>

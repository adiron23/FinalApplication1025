<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
    "תעודת הזהות" של האפליקציה מול אנדרואיד – הוא מגדיר אילו הרשאות האפליקציה צריכה,
    אילו מסכים ומקלטים קיימים בה, מהו מסך הפתיחה, ואיזה מסך נפתח בלחיצה על קישור הזמנה.
    -->

    <!-- הרשאה לשליחת התראות (אנדרואיד 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- הרשאה לתזמון התראות מדויקות בשעה מוגדרת -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- הרשאה לקבלת אירוע הפעלה מחדש של המכשיר (לתזמון התראות מחדש) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- הרשאות לקריאה וכתיבה ביומן המכשיר (סנכרון אירועים) -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <!-- הרשאה לשימוש במצלמה לצילום תמונת פרופיל -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalApplication">

        <!-- מסך הפתיחה – נקודת הכניסה הראשית לאפליקציה -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- מקלט לשינויים בחיבור לאינטרנט -->
        <receiver
            android:name=".ConnectionReceiver"
            android:enabled="true"
            android:exported="true" />

        <!-- מקלט להפעלת התראות משימות בזמן שנקבע -->
        <receiver
            android:name=".TaskAlarmReceiver"
            android:exported="false" />

        <!-- כל שאר מסכי האפליקציה (לא נגישים ישירות מבחוץ) -->
        <activity android:name=".CalendarActivity"     android:exported="false" />
        <activity android:name=".TasksActivity"        android:exported="false" />
        <activity android:name=".ProfileActivity"      android:exported="false" />
        <activity android:name=".ShoppingListActivity" android:exported="false" />
        <activity android:name=".RegisterActivity"     android:exported="false" />
        <activity android:name=".LogInActivity"        android:exported="false" />
        <activity android:name=".MainActivity"         android:exported="false" />

        <!-- מסך שער המשפחה – נגיש גם מקישור חיצוני (deep link) מסוג familyapp://join -->
        <activity
            android:name=".FamilyGatewayActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="familyapp" android:host="join" />
            </intent-filter>
        </activity>

        <activity android:name=".CreateFamilyActivity" android:exported="false" />

        <!-- ספק קבצים לשיתוף תמונות מצלמה בצורה מאובטחת עם אפליקציות אחרות -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.finalapplication.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>